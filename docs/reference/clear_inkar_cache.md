# Clear INKAR Cache

Clears the persistent disk cache used for API responses (like time
reference metadata).

## Usage

``` r
clear_inkar_cache()
```

## Value

No return value, called for side effects.
